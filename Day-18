# Count the number of divisors of a number:

class Solution:
    def count_divisors(self, n:int):
        count = 0
        for i in range(1, n+1):
            if n % i == 0:
                count += 1
        return count

n = int(input())
s = Solution()
print(s.count_divisors(n))