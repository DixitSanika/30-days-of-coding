# Find all subarrays with zero sum:

def zero_sum_array(arr):
    n = len(arr)
    res = []
    for i in range(n):
        total = 0
        for j in range(i, n):
            total += arr(j)
            if total == 0:
                res.append((i,j))
    return res

